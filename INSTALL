= INSTALLATION =

== What you need ==
 * gcc >= 4.7
 * glibc++
 * Lua >= 5.1
 * CMAKE >= 2.8

== How to install ==
 * cp <this directory>
 * mkdir bin && cd bin
 * cmake ../src
 * make
 * sudo make install
